[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    unit: Unit tests (mock all externals)
    integration: Integration tests (real DB, mocked Stripe)
    e2e: End-to-end tests (real DB, real Stripe test mode)
    slow: Tests that take longer than 5 seconds
